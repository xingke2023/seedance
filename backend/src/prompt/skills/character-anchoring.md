---
name: character-anchoring
title: 角色锚定（有参考人像时）
when_has_subjects: true
priority: 25
when: 传了 subject_definitions —— 有真实人像要锁住，脸漂了整条片子就散了
source: 主体定义句式/多图分工/标签唯一性来自用户提供的 Seedance 2.0 官方提示词指南；逐镜重写、锁定短语、单参考图工作流、抗漂移兜底来自 OpenMontage .agents/skills/seedance-2-0/SKILL.md
---
## 角色锚定（本片绑定了参考人像）

1. **主体定义单独成一段，写在开场声明之后、场景描写之前**——这一镜出场的每个主体
   各占一句，连成一段，段落里只有主体定义，不掺场景、不掺动作：

   ```
   One continuous shot, no cuts, …（开场声明）
   将@图片1中<角色1的外貌原文>定义为<主体1>；将@图片2中<角色2的外貌原文>定义为<主体2>。
   （空一行或接着写）场景：… 0-4s: … 4-9s: …
   ```

   外貌**逐字照抄给你的那行角色原文**（原文写多长就抄多长，写了发型/体型/配饰/气质
   就全都抄进去），不要改写、不要删节、也不要补原文没有的细节（原文没写「袖口挽起」
   就别写）。原文越详细，这一段就越长——**这是对的，不要为了简洁而压缩**：
   每一镜都是独立生成，这段文字是模型这一镜里唯一能依据的长相说明。
   跨镜不要用「他」「同一个人」，Seedance 每一镜都是独立生成，上一镜的定义一点都带不过来
2. **同一个角色跨镜用同一套定义文字 + 同一个标签**，也始终引用同一组图片编号 ——
   不要这一镜 `@图片1` 下一镜换成 `@图片2`，也不要这一镜叫 `<主体1>` 下一镜改叫「那个男人」。
   **标签编号 = 图片编号**：`@图片2` 的角色只能叫 `<主体2>`。
   ⚠️ 定义句是后端逐镜统一贴的原文（贴之前会把你写的那句整句换掉、标签编号也会归一），
   照抄原文能让两边完全一致；自由发挥的部分会被丢掉，写了也白写
3. **一个角色可以绑多张图，但必须写清每张图管什么** —— 不写分工，模型会把两张图揉成一张脸：
   `将@图片1中的面部特征、@图片2中的妆造与体态定义为<主体1>`
   （大头照管脸，全身照管妆造）。分工一旦定下就每镜照抄，不要中途对调。
   ⚠️ 人物**只用大头照 + 全身照两张，禁用三视图/多视图** —— 多视图会触发 ID 漂移和双胞胎
4. **定义里只放不随剧情变的静态特征**（发型发色、五官体型、衣着款式与颜色、
   随身配饰、体态气质），不要拿表情、动作、临时道具去定义 —— 那些一镜一变，
   定义就锚不住。**特征给多少写多少**：原文里的每一条都照抄，别自作主张挑三个代表；
   这一镜里人物当下的表情和小动作写在后面的时间轴节拍里，不要混进定义句
5. **多个角色要分别定义**，标签唯一且稳定：
   `将@视频1中的高个子男人定义为警察，将另一个矮个子男人定义为小偷` ——
   之后全片提到这两人一律只用「警察」「小偷」
6. **定义可以长，但不能自相矛盾**：同一个角色不要出现互相打架的特征
   （又说穿红裙又说穿风衣）；两人的**空间关系**能靠参考图表达就不要写成一长串文字
   —— 该省的是空间关系这类话，不是外貌特征
7. **角色引用尽量往前写**：脸是最需要精准还原的东西，把 `@图片N` 放进这一镜
   主体描述的第一句，别堆到句尾（见 seedance-2-0-prompting 的「重要素材前置」）
8. **锁定短语要堆**（几句一起用比单用一句稳）：the same character,
   maintain exact appearance from the reference image, no deformation, no drift,
   no face morph, do not alter clothing category or primary color
9. **服装按剧情要换时**，在它出现的每一镜都把新服装写全，并保留「不要改变服装类别和主色」
   这句 —— 否则模型会在下一镜把人换回参考图里的衣服
10. **人脸要看得清才锁得住**：给角色的镜头用中景到特写，避免逆光剪影、大远景、
   侧后方背影 —— 脸只占几十个像素时模型会照着那团模糊重新编一张脸
11. **同框人脸不要超过两张**：多人同框是最容易漂的场景，第三个人给背影、画外音或让他出画
11. **两个人以上同框必须挂双胞胎兜底**（见 default-guardrails）：
    模型很容易把两个角色渲成同款分身，尤其是外貌描述写得接近的时候
